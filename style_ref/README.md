# style_ref/ — originals (not mint assets)

Original trait art used **only** as design reference when redrawing onto `layers/base/master_base.png`.

Do not composite these directly for the collection.
